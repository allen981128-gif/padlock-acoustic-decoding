# Data availability

Large raw acoustic datasets are not stored in this Git repository.

The dissertation dataset archive contains Dataset v1, historical validation data, Binding scans, W3 unlock data and closed-loop system runs. Dataset v1 alone contains 8,400 candidate WAV records grouped into 840 complete ten-candidate decisions.

The dataset may be archived or shared separately subject to supervisor approval and the project's responsible-release decision.
